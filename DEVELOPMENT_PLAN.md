# 人生小票机开发计划

## 1. 开发目标

基于 `PRD.md` 开发一个纯前端 MVP 网站。用户可以输入一句今天发生的小事，生成一张荒诞、温柔、适合保存分享的“人生小票”。

第一版以快速上线验证玩法为目标，不接入后端、数据库、登录系统或 AI API。

## 2. 技术方案

### 推荐实现

- HTML
- CSS
- JavaScript
- Canvas 或 DOM 转图片方案

### 架构原则

- 纯静态页面，可直接部署到 GitHub Pages、Cloudflare Pages、Vercel 或 Netlify
- 所有生成逻辑在浏览器本地执行
- 用户输入不上传服务器
- 第一版优先保证体验完整，再扩展复杂功能

## 3. 建议文件结构

```text
/
├── index.html
├── PRD.md
├── DEVELOPMENT_PLAN.md
├── src/
│   ├── styles.css
│   ├── app.js
│   ├── receipt-generator.js
│   ├── receipt-templates.js
│   └── export-image.js
└── assets/
    └── favicon.svg
```

## 4. 开发阶段

### Phase 1: 项目骨架

目标：搭建可运行的静态页面基础。

任务：

- 创建 `index.html`
- 创建 `src/styles.css`
- 创建 `src/app.js`
- 完成页面基础结构
- 引入 CSS 和 JS
- 确认浏览器可直接打开运行

交付标准：

- 页面能正常打开
- 能看到标题、输入框、按钮和小票预览区域
- 桌面和移动端没有明显错位

### Phase 2: 页面布局与视觉

目标：完成第一版可用 UI。

任务：

- 实现深色背景和小票视觉
- 实现桌面端左右双栏布局
- 实现移动端上下布局
- 设计输入区、快捷按钮、主按钮
- 设计小票卡片、操作按钮和底部说明
- 添加基础 hover、active、focus 状态

交付标准：

- 页面第一屏就是可玩的生成器
- 小票视觉有热敏纸质感
- 按钮和输入框在手机端可舒适点击

### Phase 3: 小票生成逻辑

目标：完成本地规则生成。

任务：

- 创建 4 种小票风格模板
  - 温柔便利店
  - 离谱事务所
  - 宇宙收银台
  - 打工人补给站
- 建立文案池
- 实现关键词识别
- 根据输入匹配推荐风格
- 生成小票编号、日期、项目明细、合计、留言和结束语
- 支持重新随机生成

交付标准：

- 用户输入后可生成完整小票
- 同一句输入多次生成有变化
- 至少支持 4 种风格
- 未匹配关键词时可随机选择风格

### Phase 4: 交互状态

目标：让网站有完整的可玩流程。

任务：

- 输入框自动聚焦
- 输入过短时显示提示
- 点击“打印小票”进入 loading 状态
- 添加打印中按钮文案
- 添加小票滑出动画
- 快捷灵感按钮可填入示例文本
- 实现“重新打印”
- 实现“换个风格”
- 实现复制小票文字
- 显示保存成功和复制成功提示

交付标准：

- 用户从输入到生成的流程顺畅
- 错误提示清楚但不打断体验
- 操作按钮状态明确

### Phase 5: 保存图片

目标：支持用户保存小票为 PNG。

任务：

- 评估使用 Canvas 直接绘制或 DOM 转图片
- 实现小票区域导出 PNG
- 处理移动端导出尺寸
- 添加失败提示
- 测试 Chrome、Edge 和手机浏览器表现

交付标准：

- 点击“保存图片”可下载或保存 PNG
- PNG 内容完整，不裁切核心信息
- 图片宽高适合社交分享

### Phase 6: 体验打磨与测试

目标：达到可公开分享的 MVP 水平。

任务：

- 扩充文案池，降低重复感
- 检查文本溢出
- 检查移动端布局
- 检查键盘操作和可访问性
- 检查复制、保存、重新生成和换风格功能
- 移除调试信息
- 准备部署说明

交付标准：

- 核心功能全部通过验收
- 页面无明显错位、遮挡或按钮不可点问题
- 可以部署为静态网站

## 5. 任务优先级

### P0 必须完成

- 页面基础结构
- 文本输入
- 打印小票
- 小票生成逻辑
- 4 种风格模板
- 重新打印
- 移动端适配

### P1 强烈建议完成

- 换个风格
- 复制文字
- 保存图片
- 打印动画
- 快捷灵感按钮

### P2 可后续优化

- 更多风格
- 更多文案池
- 节日限定模板
- 本地历史记录
- 分享海报样式

## 6. 开发顺序

1. 搭建 HTML/CSS/JS 骨架
2. 完成静态页面视觉
3. 写死一张示例小票
4. 实现输入校验和打印按钮
5. 实现随机小票生成
6. 接入 4 种风格模板
7. 实现重新打印和换风格
8. 实现复制文字
9. 实现保存图片
10. 做移动端和细节测试

## 7. 验收清单

- [ ] 页面可直接打开运行
- [ ] 用户可以输入文本
- [ ] 输入过短时出现提示
- [ ] 点击“打印小票”可生成结果
- [ ] 小票包含完整结构
- [ ] 至少支持 4 种风格
- [ ] “重新打印”可生成不同内容
- [ ] “换个风格”可切换模板
- [ ] “复制文字”可复制完整小票
- [ ] “保存图片”可导出 PNG
- [ ] 桌面端布局正常
- [ ] 移动端布局正常
- [ ] 无后端依赖

## 8. 风险控制

| 风险 | 影响 | 应对 |
|---|---|---|
| 文案重复 | 用户新鲜感下降 | 增加文案池，随机组合多个模块 |
| 导出图片兼容性差 | 分享能力受影响 | 优先选成熟方案，必要时提供复制文字兜底 |
| 输入太开放 | 生成内容不贴切 | 用关键词匹配加随机兜底 |
| 移动端小票过宽 | 截图分享体验差 | 限制小票最大宽度，移动端使用固定比例 |

## 9. MVP 完成定义

当用户可以在手机或桌面浏览器完成以下流程时，MVP 视为完成：

1. 打开网站
2. 输入一句今天发生的小事
3. 生成一张完整人生小票
4. 重新生成或切换风格
5. 复制文字或保存图片

第一版完成后，可以邀请 5-10 个朋友试用，重点观察他们是否会连续生成、截图和分享。
